# PyTorch (install separately with CUDA support)
# RTX 50 series (Blackwell) requires CUDA 12.8+:
#   pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu128
# RTX 30/40 series (Ampere/Ada) can use CUDA 12.4:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

# ExLlamaV2 Inference Engine
exllamav2

# Hugging Face
transformers
huggingface_hub
accelerate

# UI Framework
PyQt6

# Windows Integration
keyboard
uiautomation
pyperclip
psutil
pywin32
